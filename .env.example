# OpenBeta PostgreSQL Migration - Environment Variables
# Copy to .env and fill in your values

# =============================================================================
# Supabase (for seed script and direct DB access)
# =============================================================================
# Get these from: Supabase Dashboard → Settings → Database
PGHOST=aws-0-us-west-2.pooler.supabase.com
PGPORT=5432
PGDATABASE=postgres
PGUSER=postgres.your-project-ref
PGPASSWORD=your-database-password

# =============================================================================
# UI Environment Variables (ui/tanstack/.env)
# =============================================================================
# Get these from: Supabase Dashboard → Settings → API
# VITE_SUPABASE_URL=https://your-project-ref.supabase.co
# VITE_SUPABASE_ANON_KEY=your-anon-key
